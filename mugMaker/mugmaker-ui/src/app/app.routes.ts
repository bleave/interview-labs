import { Routes } from '@angular/router';
import { MugMakerComponent } from './features/mug-maker-component/mug-maker-component';

export const routes: Routes = [
  { path: '', component: MugMakerComponent }
];
